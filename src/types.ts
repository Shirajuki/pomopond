export enum ScreenEnum {
  Login,
  Pomodoro,
  Calendar,
  Gallery,
  Settings,
}
export enum PopupEnum {
  Stats,
  Profile,
}
export type IconType = {
  color?: string;
};

export enum PomodoroStatus {
  Pomodoro,
  ShortBreak,
  LongBreak,
}
