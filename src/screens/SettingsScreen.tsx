import { h } from 'preact';
import { useState } from 'preact/hooks';

const SettingsScreen = () => {
  return (
    <>
      <div class="screen">settings</div>
    </>
  );
};
export default SettingsScreen;
